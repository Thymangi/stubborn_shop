{# templates/emails/verify_email.html.twig #}

<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Confirmation de ton adresse e-mail</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            color: #333;
            background-color: #f9f9f9;
            padding: 20px;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            background: #ffffff;
            border-radius: 8px;
            padding: 30px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }
        .btn {
            display: inline-block;
            background: #28a745;
            color: white;
            padding: 12px 20px;
            text-decoration: none;
            border-radius: 6px;
            font-weight: bold;
        }
        .btn:hover {
            background: #218838;
        }
        .footer {
            margin-top: 20px;
            font-size: 12px;
            color: #888;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Bonjour {{ user.email }},</h2>

        <p>Merci de t’être inscrit sur <strong>Ma Boutique</strong> 🎉</p>

        <p>Pour activer ton compte, clique sur le bouton ci-dessous :</p>

        <p style="text-align: center;">
            <a href="{{ signedUrl }}" class="btn">Confirmer mon compte</a>
        </p>

        <p> Attention : ce lien expire le <strong>{{ expiresAt|date('d/m/Y H:i') }}</strong>.</p>

        <p>Si tu n’es pas à l’origine de cette demande, ignore ce message.</p>

        <div class="footer">
            <p>&copy; {{ "now"|date("Y") }} Ma Boutique – Tous droits réservés.</p>
        </div>
    </div>
</body>
</html>

